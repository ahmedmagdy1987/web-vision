// Empty stub aliased in place of the `server-only` marker package when running
// unit tests (vitest runs in a plain node env without the react-server export
// condition, where importing `server-only` would otherwise throw).
export {};
